# 工程规范

## 回复语言

- 默认使用中文回复

## 项目边界

- 本仓库使用 Node.js 24、pnpm 11 workspace 与 Turbo，依赖和根脚本统一在根目录维护
- `packages/*` 只承载可复用的框架能力，`projects/*` 只承载可运行的服务端、客户端和管理端应用
- 跨 workspace 必须从包的显式公开出口导入，不得引用其他包的内部源码路径
- 公开符号通过明确的 `src/index.ts` 导出，不使用隐式深层导入作为稳定接口
- 不新增与当前需求无关的基础设施、抽象层或空目录

## 注释与 TypeScript

- 中文注释末尾不写句号，按类型、常量、核心函数、内部函数组织文件结构
- 公共类型、接口、导出函数与复杂状态必须写清职责和边界，不重复解释代码字面含义
- 使用严格 TypeScript，优先推断局部类型；公共对象结构优先用 `interface`
- 不使用 `any` 绕过类型检查；不确定的外部输入先收窄或校验

## React 与样式

- React 组件使用函数声明，回调使用箭头函数，TSX 文件只承载界面与紧邻的交互逻辑
- Hook 只在组件或自定义 Hook 顶层调用，保持稳定且可读的调用顺序
- 样式优先使用 Tailwind 工具类；共享样式和主题变量放入 `@socilab/shared-ui`
- 仅维护浅色主题基线，不为尚未定义的界面预建组件库或业务布局

## 安全与质量

- 不提交密钥、口令、访问令牌或真实个人信息，环境变量样例只保留字段名和安全的占位值
- 错误响应不得向用户暴露内部堆栈、配置内容或敏感输入
- 删除或覆盖文件前先核对绝对路径和影响范围，只操作已确认的最小目标
- 不手工运行带自动修复的检查命令；提交时由 Git hook 对已暂存的脚本文件执行格式修复

## 测试与提交

- 实现行为变更先写能失败的测试，再写最小实现使其通过；配置和纯文档用可观察的命令验证
- 测试按领域放入 `tests/<domain>/`，不要在 `tests/` 根目录放测试文件
- 生成的 `routeTree.gen.ts` 不手工编辑，且必须排除于格式和质量检查
- 提交信息使用中文 conventional commits，例如 `feat: 建立请求链`
