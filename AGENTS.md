<!-- intent-skills:start -->
## Skill Loading

在为 SociLab 执行实质性开发任务且修改文件前：

- 从 workspace 根目录运行 `pnpm dlx @tanstack/intent@latest list`，动态查看当前依赖实际提供的本地 skill
- 只有已列出 skill 的 package、关注点与当前修改意图精确匹配时，才运行 `pnpm dlx @tanstack/intent@latest load <package>#<skill>`
- 修改 Client/Admin 的 TanStack Router 或 TanStack Query 能力时，也先按当前具体问题匹配列表结果，不因项目使用了 TanStack 就加载无关 skill
- 修改时遵循已加载的 `SKILL.md`；不能把未加载、未列出或只部分匹配的 skill 当作规则来源
- 跨 package 修改时仍从 workspace 根目录执行检查，并优先选择属于被修改 package 的本地 skill
- 多个 skill 匹配时，优先加载与当前 package 或关注点最具体的一个；只有任务确实跨多个 package 或关注点时才加载额外 skill
- `list` 或 `load` 失败时，说明失败命令和原因，再依据当前 `AGENTS.md`、源码、配置与测试继续；不得假设未加载 skill 的内容，也不得新增或虚构本地 intent 配置

<!-- intent-skills:end -->

<!-- cspell:words CodeGraph -->
<!-- cspell:words liuqi qygent -->

# 工程规范

## 回复语言

- 默认使用中文回复

## qygent 工程参考基线

- 本项目以本地 `/Users/liuqi/code/qygent` 的当前代码、配置、测试和工程文档作为强制工程参考实现；用户明确要求和 SociLab 业务文档决定产品目标、业务流程、业务逻辑与业务算法，qygent 决定这些目标之外的工程实现方式
- 用户对可参考内容、无需能力和项目化适配的列举都只作为示例，不构成完整清单；每项任务必须根据两个项目的当前事实自行分析适用范围，不能把示例、目录名称、历史印象、摘要或既有差异直接当作结论
- 设计或修改前，先完整阅读 SociLab 与任务相关的业务文档、源码、配置和测试，再检查 qygent 对应实现及其上下游调用、包出口、依赖、脚本、测试和工程文档；qygent 存在 `.codegraph/` 时先使用 CodeGraph 理解代码关系，不能只对照少量同名文件
- 除经分析确认属于业务差异、项目不需要的产品能力或必要项目化适配外，文件结构、目录结构、代码结构、命名、注释、类型、模块与公开出口、依赖选择、技术栈、UI 组件库、样式体系、状态管理、路由、请求链、构建脚本、分包逻辑、测试组织、工具链和质量门禁等全部工程维度都必须与 qygent 保持一致；此处列举仍不是封闭清单
- 判断某项 qygent 能力是否适用于 SociLab 时，必须结合两边的业务目标、运行平台、数据所有权、依赖边界和生命周期给出代码或文档证据；现有实现与 qygent 不同本身不是保留差异的理由，也不能仅因能力位于插件、桌面端、Agent 或其他特定目录就直接排除
- 必要适配必须保持最小范围，复用 qygent 的职责划分、依赖方向、命名方式、实现模式和验证方式；不得借项目化适配引入另一套技术方案、平行 API、兼容层、一次性抽象或与 qygent 不一致的新规范
- qygent 没有直接对应实现时，必须继续查找职责最接近的模块和同层实现，依据其架构原则、代码组织、命名和测试方式推导方案；不能因为缺少同名功能就自由设计或降低一致性要求
- 开发完成后同时核对 SociLab 业务边界和 qygent 工程一致性，并在任务交付中说明主要参考的 qygent 文件或模块，以及所有排除项和适配项的依据；如果完整分析后仍存在会改变架构、技术栈、分包或公共契约的歧义，先向用户说明证据、取舍和影响范围再实施

## 界面文案

- 用户界面的标题、按钮、说明、提示等文案结尾不写标点符号
- 面向用户的标题、说明、状态、错误和操作文案优先描述用户目标、可获得的能力、实际影响和下一步操作，尽量不暴露内部实现细节
- 除非诊断问题、安全确认或高级配置确有必要，不在用户文案中直接使用进程、PATH、RPC、轮询、协议、内部工具或内部目录结构等实现概念
- 必须说明技术边界时，将实现约束转换成用户可理解的结果，例如使用“重新打开终端后生效”，不使用“进程环境变量已更新”
- 面向用户的错误和可恢复异常统一按“原因 → 影响 → 操作”组织，不暴露内部堆栈、配置、错误码或敏感输入
- 同一异常在页面、弹窗和提示中的核心表述保持一致，只能按容器信息密度裁剪，不能改变原因、影响或操作语义

## Git 提交与工作区保护

- Git 提交信息默认使用中文，避免使用英文描述
- 提交格式遵守 conventional commits，例如 `feat: 中文描述`、`fix: 中文描述`；type/scope 使用英文约定，冒号后的主题默认中文
- 提交前不手工执行 `eslint --fix`、`pnpm lint:fix` 或同义自动修复命令；`.husky/pre-commit` 由 `lint-staged` 对已暂存的 JavaScript / TypeScript 文件执行 `eslint --cache --fix`
- 同一文件同时存在已暂存与未暂存改动时，提交前分别核对 index 和工作区；`lint-staged` 会临时隐藏未暂存内容，提交后必须确认这些内容已恢复
- 开始修改前先检查工作区，现有修改和未跟踪文件默认属于用户；不覆盖、不清理、不暂存与当前任务无关的内容
- 提交前分别核对 `git diff`、`git diff --cached` 和 `git status --short`，只提交当前任务明确涉及的最小文件集合
- 除非用户明确要求，不执行 `git push`；禁止强制推送、重写用户提交或擅自修改分支上游
- 不提交构建产物、缓存、本地环境文件、密钥、访问令牌、真实个人信息或未跟踪的 `.codegraph/`

## Superpowers 工作文件

- Superpowers 的所有过程文件统一保存在已忽略的 `.superpowers/` 中，不写入 `docs/`
- 设计说明和实施计划保存在 `.superpowers/plans/YYYY-MM-DD-<task-name>/`
  - 设计说明使用 `design.md`
  - 实施计划使用 `plan.md`
- Subagent-Driven Development 的任务状态、工作目录和临时产物保存在 `.superpowers/sdd/<task-name>/`
- 其他 Superpowers 子目录仅在存在真实用途时创建，不预建空目录
- `.superpowers/` 不得暂存或提交
- 不创建或使用 `docs/superpowers/`、`docs/plans/` 保存任务过程文件

## 项目环境与命令

- 当前仓库使用 Node.js `24.18.1`、pnpm `11.18.0` workspace 与 Turborepo，依赖和根脚本统一从根目录 `package.json` 维护
- 不新增 `npm`、`yarn` 相关锁文件或脚本说明，跨 workspace 命令优先通过根脚本或 `pnpm --filter` 执行
- 根工作区只包含 `packages/*` 和 `projects/*`；`packages` 承载可复用框架能力，`projects` 承载可运行应用
- 服务端开发从根目录执行 `pnpm dev:server`，该命令只读取当前终端已经设置的环境变量，不自动加载 `projects/server/.env`
- 构建完成后执行 `pnpm start`，该命令通过 `--env-file-if-exists=.env` 读取 `projects/server/.env`，环境变量未配置时由程序使用 `127.0.0.1:4317` 等安全回退值
- 客户端从根目录执行 `pnpm dev:client`，默认监听 `4318`；管理端执行 `pnpm dev:admin`，默认监听 `4319`
- 客户端和管理端开发服务器把 `/api` 代理到服务端；生产环境默认使用同源 API，只通过非空 `VITE_API_BASE_URL` 显式覆盖
- 常规验证优先使用根目录脚本：`pnpm test`、`pnpm test:linter`、`pnpm typecheck`、`pnpm lint`、`pnpm spellcheck`
- 只改前端界面且确需人工确认交互时，可以同时启动对应前端和服务端；如果已有服务占用端口，先确认进程用途，再选择复用或调整端口
- `.env`、`.env.local` 等本地环境文件由 `.gitignore` 忽略，说明配置时只引用 `.env.example`，不把真实配置写进文档、提交信息或回复

## 项目边界与架构演进

- 当前阶段只维护三端基础框架和唯一的 `meta.info` 技术契约，不实现登录、课程、用户、权限、作业或其他业务页面和逻辑
- 不引入与当前三端技术骨架无关的运行形态、领域框架、持久化设施或其他项目品牌残留
- `packages/api` 负责共享契约与 Schema，`packages/request` 负责统一请求和错误边界，`packages/sdk` 负责类型化客户端，`packages/shared` 负责环境无关工具，`packages/shared-ui` 负责共享主题、样式和 UI 能力
- `projects/server` 只承载服务端应用，`projects/client` 只承载客户端应用，`projects/admin` 只承载管理端应用；可复用能力不得长期沉积在应用目录
- 新功能、问题诊断和重构先根据当前源码、配置和测试确认真实边界，不根据记忆猜测接口、状态或生命周期
- 发现现有边界无法支撑目标时优先修正职责和数据所有权，删除重复实现、无效封装和残留分支，不通过新增兜底状态、旁路结构或一次性 helper 叠加补丁
- 当前项目处于未发布开发阶段，内部架构变更默认不保留兼容；旧出口、旧类型和旧入口直接删除并同步全部调用方与测试，除非用户明确要求兼容
- 当前阶段以职责单一、依赖清晰和总代码量可控为优先级，不为未来可能需要的能力预建兼容层、空抽象、占位实现或空目录
- 用户体验与核心结构冲突且无法低成本兼顾时，可以先缩小体验范围，不为保留表面行为引入错误抽象或冗余状态
- 修复问题必须定位设计源头、数据所有权或生命周期根因；现有边界无法支撑正确语义时直接调整边界，不增加补丁分支和一次性状态掩盖根因
- 同一能力只保留一套清晰 API，不为了兼容或显得完整并行暴露重复的注册、触发或访问入口
- 每轮功能完成后检查重复实现、无效封装、薄转发层、空源码目录和可删除代码，发现后在当前范围内直接清理
- 能从源码、配置和测试确认的事实先主动探索；涉及产品意图、架构取舍或高影响决策且无法从仓库确定时，及时询问用户，不静默猜测或切换到实质不同的实现
- 发现更优方案时主动说明收益、代价与影响范围，让用户能够在实施前核对取舍
- 问题诊断、新需求方案、架构、重构和全仓治理计划默认按 P0、P1、P2 三个可独立验证和提交的批次组织
- 事务、并发、流式状态或共享生命周期无法安全并入前三批时最多增加到 P3；P 级表示实施顺序，不表示严重度或文件大小
- 重大架构调整或核心重构至少进行三轮独立复查：第一轮核对命名、文件、目录、出口、Schema、测试和文案残留
- 第二轮核对包依赖、状态与 Context 所有权、重复能力、一次性 helper/class、薄转发层和空源码目录
- 第三轮沿 `Server → API → Request → SDK → Client/Admin` 完整链路核对启动、请求、错误、重试、刷新和失败路径；每轮发现问题后先修复并重跑该轮

## 安全边界

- 清理缓存、临时目录或生成物前必须先确认 `pwd`、目标绝对路径和目录内容，不能凭变量名或记忆直接删除
- 不使用宽泛的 `rm -rf` 清理数据；必须删除时只删除明确的叶子目录或文件，并优先使用 `ls` / `find` 展示目标
- 删除用户目录、仓库根目录、`projects`、`packages`、`docs` 等高风险路径前必须向用户确认，不能把局部清理扩大为删除源码或工作区
- 涉及删除、迁移、重命名目录时，先说明目标路径和影响范围，再执行最小范围操作
- 删除或覆盖文件前核对绝对路径和影响范围，只操作已确认的目标；优先使用可恢复操作
- 运行数据、缓存和项目源码必须严格区分；清理生成物不能触碰 `packages`、`projects`、`README.md`、配置文件或业务文档
- 不提交密钥、口令、访问令牌、真实个人信息或包含这些内容的日志；环境变量样例只保留字段名和安全占位值
- 外部输入在 HTTP、配置和文件等边界先收窄或校验，错误响应不得暴露内部堆栈、配置内容或敏感输入
- 检查因缺少外部服务、密钥或端口占用失败时，不用兜底代码掩盖，记录失败命令、原因和已经通过的边界

## 注释风格

- 移植或比照第三方源码时，在文件开头写明仓库、版本和具体文件路径，并说明原实现机制、项目改动、响应式依赖、回调更新、SSR/浏览器兼容与错误边界；不写无法逐项核对的笼统“参考某库”
- 第三方源码只作为实现证据，不为比照而新增运行时依赖；第三方许可声明文件由用户维护，除非用户明确要求，不创建或修改
- 函数声明、class 方法和有实现体的箭头函数上方的块注释只写职责，通常只写一行，不在职责注释里展开流程细节
- 函数声明、class 方法和文件级箭头函数使用多行块注释，即使只有一行说明也写成三行

```typescript
/**
 * 启动服务并返回关闭句柄
 */
export function startServer() {}
```

- 函数内部局部箭头函数的职责注释统一使用单行块注释 `/** ... */`，不按复杂度扩成三行
- `useMutation` 的 `mutationFn`、事件处理函数和局部 helper 只要有实现体，也按函数规则写职责块注释，不使用 `// xxx` 充当标题
- 类型、接口、常量、普通字段、接口成员和对象类型成员需要写职责注释，并使用单行块注释 `/** ... */`
- 接口里的函数类型成员和回调字段也按字段声明处理，默认使用单行块注释
- 公共类型、公共接口、导出函数与复杂状态必须写清职责和边界，不重复解释代码字面含义
- `useState`、`useRef`、`useMemo` 等声明状态、引用或派生值时，语义不明显就用单行块注释说明
- Hook 解构、网络请求结果和重要派生值如果牵涉共享状态、SDK 投影或本地 UI 状态，必须说明数据语义和权威来源
- 函数内部需要补充说明时使用 `//` 行注释，解释实现意图、边界条件、状态变化或失败原因，不把行注释当函数标题
- 复杂函数的执行流程、数据来源、合并顺序、并发边界和失败边界放在函数体最上方或关键分支前，不堆在函数职责注释里
- 代码稍微复杂后主动补充必要注释，说明模块职责、执行顺序、数据流转、边界条件和设计意图
- 状态同步、缓存合并、服务端结果校准、异步竞态和协议转换等容易误读的逻辑，要说明权威来源、覆盖顺序和失败时保留的状态
- 组件同时使用 SDK 结果、TanStack Query 缓存、React state 或浏览器本地存储时，注释说明每类数据的所有权；服务端结果负责最终校准，本地 state 只负责临时界面状态
- 多数据源合并时主动说明合并顺序和覆盖规则，不让后续维护者把派生值误写成需要同步的第二份状态
- 异步函数存在过期请求、后台任务、重试、兜底缓存或失败后保留旧状态时，在关键分支说明触发条件和要避免的问题
- 不使用带序号的步骤注释，避免代码调整后序号漂移
- 有实际实现或类型聚合的源码文件必须保留现有结构分区标题，并按文件职责补齐适用的标题；纯配置文件和生成文件不适用。标题例如 `/** -------------------- 类型 -------------------- */`、`/** -------------------- 常量 -------------------- */`、`/** -------------------- 核心函数 -------------------- */`、`/** -------------------- 工具执行函数 -------------------- */`、`/** -------------------- 内部函数 -------------------- */`
- class 内方法较多时使用 `/** -------------------- 核心方法 -------------------- */` 和 `/** -------------------- 辅助方法 -------------------- */` 区分对外主行为和内部逻辑
- 分区标题只用于文件结构，不用于函数内部步骤；标题与下一条注释之间不留空行
- 文件内容通常按类型、常量、核心函数、内部函数排列
- 中文注释末尾不写句号；注释要适量，不写“设置 name”“调用 save”这类只重复代码字面含义的内容

## TypeScript 与代码风格

- 使用严格 TypeScript，优先沿用现有结构和命名，小范围修改不顺手重构无关代码
- 优先让 TypeScript 推断返回值和局部变量类型，公共 API、`any` 风险或复杂返回边界再显式标注
- 不使用 `any` 绕过类型检查；不确定的外部输入先作为 `unknown` 收窄或通过 Zod 校验
- 对象结构优先使用 `interface`，联合类型、工具类型组合和非对象结构再使用 `type`
- 不写只包一层已有类型的空接口或别名；类型能安全内联就内联，跨多处复用或属于公共契约时再提取
- 项目自定义的目录字段、变量、函数和源码目录统一使用 `dir` / `XxxDir`；`directory` 只保留在用户文案、Node.js / 第三方固定 API 和真实外部文件名中
- 运行时代码保持 Node.js 24 兼容；Node.js 24 的 import attributes 原生只支持 `type: 'json'`，禁止使用 `import ... with { type: 'text' }` 读取文本资源
- 入口校验优先放在服务端 Schema、Zod 默认值或模块公共入口，内部调用链不重复抛出相同的缺参错误
- 能由 Zod 完成的输入校验不再手写同义 helper，避免 Schema 和分支判断形成两套事实
- 请求与用户输入按真实语义使用 `input`、`request`、`data`、`text` 等短名称，不为显得抽象添加重复的类型或模块前后缀
- 字段语义未变化时在选择边界、Hook、局部派生、对象装配和返回值中保留原名，不添加重复当前上下文的前缀
- 同一作用域既保留原始值又生成同名权威值时，前序临时值使用 `_value` 这类短名称，最终值保留自然名称并使用对象属性简写
- 只有同一作用域确实并存“当前选择值”和“已校准值”时才使用 `selectedXxx` 与 `xxx` 两个名称，不在下游再映射回原字段
- SDK 或请求动作进入编排层后与对外包装动作重名时，临时绑定使用 `_request`、`_refresh` 等短名称，对外动作继续保留自然职责名
- 从对象读取多个字段用于同名局部变量或同名对象字段时优先解构；字段需要大量重命名、默认值或生命周期判断时直接读取源对象更清楚
- 函数体顶层读取参数属性时形成连续声明区，位于 Hook 和其他运行时语句之前；条件分支专用读取与 `unknown`、`any`、可空值收窄后的读取留在窄作用域
- 解构包含少量短字段时保持单行；字段多、别名长、默认值复杂或一行拥挤时再按字段换行
- 编排函数同时接收运行选项和业务输入时，外层参数使用 `context`，入口业务输入命名为 `request`；如果还要转换为下游输入，转换结果命名为 `input`
- 字段进入下游后语义变化时在解构处直接重命名并继续使用属性简写，不把重命名拖到最终对象投影
- 字段需要默认值、布尔化或其他转换时，如果提前命名能提升可读性，先保存为最终字段名再使用属性简写；大型边界映射直接内联更清楚时保持内联
- 只有原始值和转换值需要同时存在时才使用 `inputXxx`、`rawXxx` 等来源名称，不为单纯转发创建中间别名
- 普通业务对象字段改名优先在解构处完成；选择边界允许直接映射局部 key，不为这一层再加转换函数
- 类同时提供静态 `create()` 和 `withXxx()` 时，静态入口负责规范化输入并创建共享依赖，私有构造函数只接收完整上下文，不判断当前是首次还是派生创建
- 创建完整上下文时，先在连续声明区准备入口字段、派生值、集合和依赖，最后用纯字段投影创建实例，不在装配对象里内联 `map`、数组展开或 `create()` 调用
- 空集合无法推断业务元素类型时显式标注，例如 `const origins: string[] = []`
- `withXxx()` 只重算真正变化的字段，共享生命周期资源使用同名局部变量，最终以纯字段投影创建新实例，不把 `this.xxx`、数组展开或字段改名留在调用处
- 私有构造函数先解构完整上下文，需要规范化的输入在解构处命名为 `inputXxx`，再计算最终同名变量并连续写入实例字段；不要先写 `this.xxx` 再从实例字段读取来完成同一轮装配
- 上述装配规则不适用于单次属性读取或直接委托；直接表达更清楚时不要机械增加只用一次的局部变量
- 编排函数里的短解构、短参数调用和纯字段投影保持单行，只有确实拥挤或包含复杂嵌套时才换行
- 数组解构一行放不下时，每个元素独占一行，收口写成 `] = 调用(`；不要把 `=` 单独放到下一行
- 解构不是机械要求：如果只是取出大量字段再原样拼回对象，优先直接读取源对象；只有存在公共字段投影工具时才复用，不为该场景临时新增工具
- 两个内部边界如果只能靠“解构、重命名、展开、重新拼装”来回转换，优先统一类型或删除中间层，不用映射函数隐藏重复协议
- 即使字段同名，少量字段周围混有固定值、派生值或调用依赖时也优先显式写字段，不按字段数量机械替换为投影工具
- 对状态容器、ref、缓存对象等运行中会变化的对象，不在外层提前解构易变字段；需要当前快照时在使用点窄作用域读取
- 类型断言使调用参数变长或需要 `as unknown as Xxx` 等多段断言时，先声明有语义的局部变量再传入；循环临时变量可以使用 `item`、`value` 等短名称
- 可选布尔参数默认是 `false` 时不额外写 `= false`
- Promise 必须通过 `await`、`return` 或显式拒绝处理完成归属；不使用 `void` 忽略 Promise，确需后台执行时在调用点补 `.catch()` 并处理失败
- 第三方库缺类型时优先使用官方类型或 `@types/*`，确实没有可用类型时才在当前 workspace 内补最小 `.d.ts`
- 不为一两行简单逻辑提取 helper，尤其不创建 `formatError`、`errorMessage` 等只包一层字符串或类型判断的函数
- 文件级 helper 只用于跨多处复用、隐藏稳定边界、隔离协议转换或明显改善模块结构；只被一个函数使用的局部步骤优先收进调用方
- 只调用一次的一两行简单逻辑直接内联；文件名生成、路径安全、协议解析等有明确职责和边界的逻辑可以作为调用处内部函数
- 一两行局部函数能让主流程更清楚时，定义为当前函数内的 `const xxx = () => {}`，不提升到文件级
- 普通函数内部的局部步骤使用箭头函数，React 组件事件函数使用箭头函数，与组件状态无关的转换使用函数声明并放在最近的 `utils.ts` 或实现模块
- 一个主函数包含多段可独立命名的局部逻辑时，用内部箭头函数组织，让文件级结构只呈现稳定主边界，主函数末尾保留可顺序阅读的核心流程
- helper 只服务某个内部函数时继续收进该函数，不留在更外层作用域
- 顶层函数只保留对外 API、跨多处复用的能力或稳定主流程；不因“只调用一次”机械嵌套整条复杂调用链
- 一次输入到输出的同步转换即使有临时可变状态也优先使用函数；只有对象需要身份或状态跨多个操作和生命周期持续存在时才使用 class
- 局部函数已有外层上下文时删除重复的模块、动作和类型前后缀，使用短而明确的名称；顶层函数继续使用脱离调用点也能表达完整职责的名称
- 相邻局部函数之间保留空行，职责注释紧贴对应函数
- 函数和方法参数短且清楚时保持单行；参数多、嵌套深或信息密度高时按参数逐行展开
- 单参数函数的参数类型短且一眼可读时保持单行；即使只有一个参数，类型或返回值嵌套很深时也可以换行
- 对象解构参数优先在括号内展开字段，类型标注跟在收口花括号后
- 函数调用参数较多或较长时逐个换行；单参数调用、短参数调用和短高阶函数回调优先保持单行
- 语句过长时通过参数、泛型、链式调用、高阶函数回调换行或语义局部变量改善可读性；一行不同时塞入过多嵌套调用和组合条件
- 换行不是固定规则：拆行造成语义分散或明显长短失衡时重新调整，短调用、短条件和短链式表达式单行更清楚时保持单行
- 简单字符串、模板字符串和 JSX prop 能在一行表达完整逻辑时保持单行，不为行长提示机械拆分
- JSX 开标签只有少量短 prop 且一行可读时保持单行；复杂 prop 先在 `return` 前声明语义变量
- 配置型数组或对象可以提取为 `items`、`columns`、`tabs` 等语义值以减少同构重复；只服务一个 prop 的一次性表达式留在调用处
- 除非能降低复杂度、减少重复、隐藏细节或表达关键语义，不为缩短一行声明只使用一次的变量或常量
- 简单判断可单行表达，复杂判断先声明布尔语义变量；`for...of` 用于副作用、异步或多步骤控制流，高阶函数用于简单映射、过滤和查找
- 空行只分隔不同语义块，连续相关语句不为“透气”额外留空行
- class 对外方法显式写 `public`，私有方法和私有字段使用 `_` 前缀
- 除错误类型、JavaScript / 平台 / 第三方对象和框架要求直接构造的对象外，业务调用方不对项目自定义 class 使用 `new`；普通实例通过 `public static create()` 创建，进程级单例通过 `public static getInstance()` 获取，构造函数保持 `private`，直接构造只留在类内部的创建或派生实现
- class 构造函数只保存入参时优先使用构造函数参数属性；使用参数属性时不逐个写注释，在 constructor 上方说明职责

## 目录、模块与公开出口

- 源码模块目录除 `index.ts` 出口外，避免同时混放普通文件和子目录；同层实现能保持单文件时使用 `[name].ts` 平铺，不创建只有一个 `index.ts` 的机械叶子目录
- 拍平目录层级不等于合并不同职责；同级存在真实多文件模块、资源目录或明确能力边界时使用 `[name]/index.ts`，当前目录只保留聚合出口
- 模块目录按真实职责创建文件：前端按需使用 `root`、`props`、`types`、`constants`、`utils`、`hooks`，服务端按需使用 `types`、`schema`、`repository`、`lifecycle`、`projection`
- 上述职责文件只有存在真实内容时才创建，不为目录对称创建空文件、空目录或薄转发层
- 辅助转换统一使用 `utils.ts`，不新增 `helper.ts`、`helpers.ts` 或 `helper/` 形成第二套命名
- `index.ts` 只承担稳定出口、薄门面或必要的主流程编排，不为了缩短入口拆出大量一次性 helper
- 生产源码禁止默认导出和任何星号导出，包括 `export * from`、`export type * from`、`export * as Xxx from`；框架要求默认导出的工程配置文件除外
- 公共出口使用 `export { ... }` 和 `export type { ... }` 明确列出契约，新增公开符号必须从对应 `src/index.ts` 显式导出
- 新增包或公开子路径时同步更新对应 `package.json#exports` 和根 `tsconfig.json#paths`；测试或应用有独立别名配置时一并更新
- workspace `tsconfig.json` 保持薄继承，只 include 当前 workspace 源码和必要配置；仅在真实依赖相邻包全局声明时扩展 include
- `packages/shared` 只承载浏览器与 Node.js 都能使用的通用类型和工具；Node.js 专属能力需要真实调用方和公开子入口后再新增，不能让共享包反向依赖 UI
- `packages/shared-ui` 只承载共享主题、样式、UI 组件与浏览器工具，不引入应用状态、SDK 查询或服务端契约实现
- `packages/api` 只声明跨端契约、Schema 和路径；`packages/request` 统一传输与错误；`packages/sdk` 组合 API 与 Request，不在 Client/Admin 重复创建第二条请求链
- 公共方法参数、返回值和回调涉及的具名契约必须从包出口显式导出；只服务内部装配的 Context、Ref、Cache 等类型留在所属模块，不因测试直读源码扩大公共 API
- 只被单个实现使用的类型留在实现文件；同一领域多文件共享类型放在模块 `types.ts`，跨模块公共契约通过领域出口导出
- 不维护混合多个领域所有者的巨型 `types.ts`，也不为单个接口创建 `Interface.ts`
- 跨 workspace 必须从 `@socilab/api`、`@socilab/request`、`@socilab/sdk`、`@socilab/shared`、`@socilab/shared-ui` 等显式公开出口导入
- workspace 之间禁止使用 `../../` 穿透源码，也禁止通过 `@socilab/<package>/src/...` 绕过公共契约
- 同一 workspace 内相邻实现可使用相对导入；包出口聚合和明确内部单向依赖不通过自包 barrel 绕行，避免循环依赖
- 同一 workspace 的组件或业务模块读取已经公开的包能力时使用包入口；内部强父子关系和局部 helper 继续使用相对路径
- `debounce`、`delay`、`deepMerge` 等通用能力优先放在 `@socilab/shared` 实现和复用，不为一两个函数引入 `lodash`、`deepmerge` 等运行时依赖，也不重复手写同义工具
- 只有一层实现且没有真实替换点时，不拆 adapter、factory 或 define 透传层，直接暴露可用边界

## React 与 TSX

- 前端沿用 React 19、TanStack Query、TanStack Router、React Compiler、Tailwind 与 `@socilab/shared-ui`，不为局部需求引入第二套 UI、路由、请求或状态方案
- React Hooks、`Fragment`、`ReactNode` 等从 `react` 具名导入，避免在 JSX 和组件逻辑里写 `React.xxx`
- React 组件使用 `function Xxx(props: XxxProps) {}` 声明，不使用 `FC`、箭头函数或函数表达式；事件和 JSX 回调继续使用箭头函数
- 泛型函数组件使用 `function Component<T>()`，不强套 `FC`，避免削弱泛型推断
- TSX 文件只承载界面和紧邻交互；纯数据转换、解析和格式化逻辑移到相邻 `utils.ts` 或所属框架包
- 一个 `*.tsx` 文件默认只放一个主组件；同一抽象层的小组件最多两个，继续增长时按真实组件边界拆分，不预建空目录
- 应用入口、路由和 TanStack Router 约定文件继续遵守框架结构；可复用叶子组件使用 `组件名/index.tsx`，不新增同级 `foo.tsx` 与 `foo/` 混放
- 组件 Props 接口使用 `[组件名称]Props`，不使用裸 `Props`；组件目录按实际内容使用 `props.ts`、`types.ts`、`constants.ts`、`utils.ts`、`hooks.ts`
- `props.ts` 只放组件 Props，`types.ts` 放非 Props 类型，`constants.ts` 放模块常量，`utils.ts` 放无 Hook 和外部副作用的转换，`hooks.ts` 放可独立命名的组件专属 Hook
- 上述职责文件只有实际内容时才创建；组件入口直接导入，不通过 `index.tsx` 反向转导类型或常量
- 一个组件模块同时包含根组件和子组件目录时，根组件收进 `root/index.tsx`，模块外层只保留 `index.ts` 稳定出口和真实子组件目录
- 组件模块的 `index.ts` 只导出根组件和确需公开的共享契约，不实现业务逻辑；上述 root 结构对子组件递归适用
- 不为普通函数、Hook、状态、类型或常量创建伪组件目录；一两行表达式直接内联，多步转换或稳定协议边界放入 `utils.ts`
- 点击、键盘、拖放和提交等依赖组件闭包的事件函数保留在组件中，不为了拆文件包装成事件 helper
- 简单 `useState`、`useRef` 和只服务一处 JSX 的 Hook 留在组件；具有完整订阅、观察和清理生命周期且可独立命名的逻辑再提取自定义 Hook
- 只被一个组件或模块使用且离开它没有独立意义的 Hook 收进调用方 `hooks.ts`；应用级 Hook 目录只保留跨多个模块共享的能力
- 非组件 Hook 或状态模块不套用组件 `root/props` 结构；按真实内容使用 `types.ts`、`constants.ts`、`utils.ts`、`hooks.ts`，`index.ts` 只保留主入口和必要转导
- Client/Admin `src` 根级公共目录只保留跨业务模块复用或应用级基础能力；单一模块依赖收进所有者，不保留根级转发文件，也不因未来可能复用而提前公共化
- 新增根级公共分类前先确认存在多个独立调用方，不按文件类型机械提升组件专属常量、类型、工具或 Hook
- 浏览器 Object URL、Observer 和订阅句柄由创建它们的状态所有者管理；创建时把句柄与业务对象绑定，在删除、清空、作用域切换、成功提交和卸载时逐一释放
- 浏览器资源不进入 API 契约、SDK 输入或持久化状态；创建前先完成参数校验，创建后未成功纳入所有者状态时立即释放
- 异步流程结束后清空资源集合时，必须明确等待期间是否允许继续增删；允许变化时读取最新集合再释放，禁止只释放异步开始时闭包捕获的旧快照后直接清空新状态
- React 状态 updater 不得执行 `revoke`、取消订阅等副作用；需要最新集合时使用只在事件或清理阶段读取的 latest ref
- 应用状态按资源所有权、投影职责和写入边界拆分，不把 SDK 数据、Query 缓存、导航弹窗、输入草稿和设置堆进一个全局状态对象
- 多层共享状态保持单向无环依赖，上游状态不读取下游状态；同一网络事实只保留一个权威来源，不在组件和缓存间复制第二份 live 字段
- 高频对象提供给低频消费者时只投影实际消费的标量或稳定子引用，不把高频集合塞进共享 metadata 逃避依赖边界
- 纯展示组件只接收叶子展示需要的 props，不强绑定应用状态或请求上下文
- 普通文本和说明优先使用 `span`、`p` 等语义 HTML 配合主题类，不为纯排版引入新组件抽象
- 不抽“按钮加图标”“提示加按钮”或简单样式包裹等薄组件；只有参数化后能脱离单一父组件且存在独立调用边界时才提升为共享组件
- 判断组件父子关系看业务所有权和调用范围，不只看视觉层级；只被父组件使用或离开父组件无独立意义的子组件放回父模块
- 父模块有多个强依赖子组件时使用 `module/index.ts`、`module/root/index.tsx`、`module/child/index.tsx`，不要在同层混放根实现和子目录
- 组件目录和文件名默认使用一到两个 kebab-case 语义段，父目录已表达的模块语义不在子目录重复
- 组件导出名和 Props 名必须脱离文件路径也能表达业务语义；`List`、`Section`、`Content` 等泛名在调用点会失去语义时保留必要业务前缀
- `Label`、`Panel`、`Container` 等后缀只有表达真实职责时保留；三段以上名称只在业务专名、协议术语或去掉后歧义时使用
- 弹出层触发器和内容属于同一功能且拆开会形成薄文件时可以放在同一文件，但仍遵守最多两个组件的上限
- 历史多组件文件不因规范自动大拆；后续真实触碰相关区域时再按单一组件和所有权边界整理
- Hook 只在组件或自定义 Hook 顶层调用，调用顺序保持为：通用 Hook → state/ref Hook → memo Hook → 普通派生值与事件函数 → `useEffectEvent` → Effect 类 Hook → `return`
- `useEffectEvent` 与 Effect 分别集中排列，不交错声明；它只供 Effect 调用，不代替点击、提交等普通事件
- 事件处理和提交动作使用箭头函数，放在 memo Hook 之后、Effect Event 与 Effect 之前；必须放在 state Hook 前的普通函数应移到 `utils.ts`
- 新增或改名 Hook 时按真实生命周期归类：返回状态或 ref 的 Hook 属于 state/ref 阶段，返回稳定动作的 Hook 属于事件阶段，建立订阅的 Hook 属于 Effect 阶段
- 不能通过 ESLint disable、白名单文件或跳过 `pnpm test:linter` 绕过 Hook 顺序与官方 rules-of-hooks
- Effect 中需要读取最新状态但不应触发重新订阅时先声明 Effect Event；它不能隐藏本应触发 Effect 重跑的响应式依赖
- 稳定订阅函数直接返回 cleanup、第三方要求原样注册回调或纯 DOM 测量等不需要最新闭包的场景，可以不使用 Effect Event；边界不明显时用函数体内注释说明
- 简单 Effect 保持单行；多步副作用或异步流程使用 Effect 内的命名局部函数，并明确清理、过期结果和失败边界
- 同一流程同时由用户动作与 Effect 触发时，先重新判断职责和状态所有权，不为复用额外制造稳定回调
- Hook 依赖数组只有一个依赖时保持单行，依赖多或名称长时再逐项换行
- 用户操作、定时器、异步任务和 Observer 通知需要稳定函数身份时使用真实依赖的 `useCallback`；普通事件交给 React Compiler，不用自定义稳定回调隐藏响应式依赖
- callback ref 或函数身份变化需要驱动重新订阅、重建资源时继续使用 `useCallback` 并保留真实依赖，不能为稳定身份隐藏重建条件
- 用户触发的异步操作、表单提交和按钮 loading 优先使用 TanStack Query `useMutation`
- `useMutation` 必须用 `mutationFn` 表达请求、用 `isPending` 投影提交状态，多参数动作合并为单个 variables 对象；校验留在提交边界，请求成功后再关闭交互容器
- TanStack Query 只管理服务端请求状态，不为了统一请求 API 把本地草稿、导航状态或纯 UI 状态迁入 Query 缓存
- 浏览器本地持久化键集中在应用级常量边界，组件和 Hook 不重复定义同义 key；命名按真实存储职责，不使用模糊的 `selection`、`data` 等名称
- 非 React 代码直接访问浏览器存储前先确认无法在 Hook 或组件状态边界处理，并用注释说明原因
- 不因为引用稳定性机械添加 `useMemo`、`useCallback` 或 `memo`；只在第三方依赖敏感、存在明确性能问题或计算成本较高时使用
- 客户端和管理端始终启用 React Compiler；普通对象、Context value、JSX 与纯派生值的稳定性默认交给 Compiler，不为它们机械手写 `useMemo`
- Effect 依赖保留领域对象边界，不为手工稳定引用把一个对象拆成大量函数和标量
- 不用永久函数记忆化、`memo` 包装或 ESLint 白名单掩盖可修复的 Compiler 问题；修改组件或自定义 Hook 后，通过对应 Client/Admin 构建实际运行 Compiler 并核对输出
- Compiler 因清晰的对象解构默认值跳过单个函数时，不为通过优化机械改写语法；只有真实渲染问题或高成本计算才手工优化该边界
- 事件处理直接调用函数，不写 `void someHandler()` 或 `void Promise`；参数能推断时不手写事件类型
- 短事件参数优先使用 `e`；只有公共 API 或框架类型边界需要时才显式标注事件类型
- JSX 中单纯的条件展示使用 `condition && <Component />`；非布尔值先显式布尔化，互斥分支继续使用三元表达式
- 静态样式、字面量和简单字段读取可以内联；有业务语义的 JSX 值先声明再传入，一两行派生值不提取薄 helper
- JSX 属性或 children 中的复杂表达式需要多行时，大括号独立换行承载表达式，不写半行后再换行
- JSX 不内联业务计算、复杂事件函数或复杂对象；按“前置守卫 → 派生值 → 事件函数 → ReactNode / Props 对象 → `return`”组织
- Effect 专属流程放回对应 Effect，复杂渲染分支优先拆组件，不抽局部 `renderXxx`
- 默认不启动浏览器工具做页面验证；只有用户在当前任务中明确要求时才执行

## Tailwind 与 className

- 业务 UI 优先使用 Tailwind utility class；只有主题入口、`@theme` / `@utility`、第三方全局覆盖或确属全局样式入口时才维护 CSS
- 共享样式和主题变量放入 `@socilab/shared-ui`，客户端和管理端不复制第二套主题；当前只维护浅色主题基线
- `styles` 等非组件资源目录按职责使用平铺文件，例如 `theme.css`、`base.css`，不为单个 `index.css` 创建机械目录
- 全局 CSS 变量只保存跨组件共享且随响应式、平台或运行状态变化的值；只在单个组件树传递的变量就近声明，使用短而明确的语义名
- 需要调整共享视觉时修改 `@socilab/shared-ui` 所属主题或组件，不在 Client/Admin 叠加全局选择器覆盖
- 字体、字重和行高默认交给全局主题与浏览器基础样式，业务组件不随手添加 `font-*`、`leading-*` 覆盖，明确品牌标题或代码展示除外
- 样式组合在多处重复且足够复杂时才考虑 `@utility`，一两行 Tailwind 组合不抽成薄 utility
- 前端布局、间距、字号、圆角和控件尺寸优先使用 Tailwind scale 或稳定主题 token，不在 JSX、`className` 和业务 CSS 中散落固定像素或一次性 `rem`
- 发丝线、动态 DOM 测量、CSS 函数和第三方组件明确比例等真实例外可以使用精确值，不机械替换
- 组件样式优先写入 `className`；只有动态数值、第三方组件只暴露行内入口或运行时计算更清楚时才使用 `style`
- `className` 足够短时保持单行，能不用 `cn()` 就不用；多 utility 静态类名在 56 个字符以内优先使用一个字符串
- `className`、`classNames` 语义槽位和样式常量中的多 utility 静态字符串超过 56 个字符时，改为多行 `cn(...)` 并按语义拆成字符串参数；单个复杂 arbitrary selector 或单个长 utility 可以保留
- 总静态样式超过 56 个字符的 `cn(...)` 也要换行，不能把长字符串原样包一层；分组不机械拆开关系紧密的 utility
- 一个静态类名字符串不使用 `cn(...)` 包装；只服务单个 JSX 片段的一次性 className 不提取成常量
- 合并多段静态或条件样式统一使用 `cn`，不使用数组 `join(' ')`、`filter(Boolean)`、字符串拼接或条件模板字符串
- 条件类名优先使用 `condition && '...'` 或条件对象，整段 class 的简单三元选择可以保留
- 静态类名不使用模板字符串或字符串拼接换行；需要换行时直接使用 `cn(...)`
- JSX 的 `className` 或 `classNames` 属性值只要跨行，属性本身就在标签名后另起一行
- 组件透传的 `className` 放在 `cn(...)` 最后，允许调用方覆盖前面的 Tailwind 冲突类
- Tailwind 尺寸、间距、圆角、字号和断点优先使用 scale utility，不使用任意像素断点；细间距使用数字 scale
- 间距不使用单像素 spacing utility，需要细间距时使用 `py-0.5`、`gap-0.5` 等数字 scale
- 只有真实依赖外部协议、第三方内部选择器或 CSS 函数无法用 scale 表达时才使用 arbitrary class；能使用 `rem`、百分比、内置断点或 scale 时不写裸 `px`
- `z-index` 按语义分层：`z-0`–`z-10` 用于组件内部布局，`z-20` 用于组件内部操作层，`z-30` 用于页面覆盖层，`z-40` 用于模态遮罩，`z-50` 只用于 Portal 内容或根定位层
- 遮罩与弹层内容使用不同层级，不依赖同一 `z-index` 下的 DOM 顺序决定点击归属；局部悬浮按钮、拖拽手柄和 sticky 标题不直接使用 `z-50`
- 新增超过 `z-50` 的可交互层前先全局核对 Portal、模态遮罩和页面覆盖层所有权，不使用 `9999` 等任意高值回避分层
- Tailwind 4 类名保持 canonical 写法，例如 `break-words` 改为 `wrap-break-word`、`aspect-[4/3]` 改为 `aspect-4/3`、`!border-0` 改为 `border-0!`
- 任意 `calc()` 类名删除只为转义空格写入的 `_`，例如使用 `top-[calc(50%-(--spacing(6)))]`
- 修改 Tailwind 类名后，优先通过 Tailwind CSS LSP 扫描 `projects/client/src` 与 `projects/admin/src`，处理 `suggestCanonicalClasses`、无效 utility 和冲突诊断
- 复用本机编辑器已有的 Tailwind CSS Language Server，不为 LSP 扫描新增项目依赖或配置
- LSP 无法识别第三方 `classNames` 语义槽位时仍要手工检查 canonical 写法、前缀式 `!utility`、旧 shorthand 和不必要的 arbitrary class
- `min-h-0` 只用于 flex/grid 子项确需突破默认最小高度承载滚动或收缩的场景，不作为普通容器默认类
- `cn(...)` 多行分组先保证语义一致，再按布局/定位、尺寸/间距、边框/圆角、字体/颜色、响应式、伪元素、状态和外部 `className` 排列
- `flex`、`grid`、定位偏移和主轴/交叉轴对齐等布局语义优先在同一组；`w-*`、`h-*`、`min-*`、`max-*`、`grow-*` 等尺寸约束单独成组
- 不破坏语义分组时让每行长度和信息密度接近，普通静态 utility 分组字符数差不超过 32；复杂 selector、arbitrary class、响应式和状态组不强制均分
- 第三方内部选择器或复杂状态类天然长短不一时不为满足差值打散语义
- 一次性 className 组合写在 JSX 或当前 `cn(...)`，只有多处复用、稳定设计 token 或复杂条件组合才提取样式常量
- `cn` 由 `@socilab/shared-ui` 统一导出并基于 `clsx` 与 `tailwind-merge` 实现，Client/Admin 不复制同名工具

## 测试与质量门禁

- 新增或修改测试前先明确它要阻止的具体生产回归；无法说明对应的行为、公共契约、失败路径、
  安全边界或已确认缺陷时，不新增永久测试
- 业务逻辑分支、状态转换、错误边界、公共契约和缺陷修复先写能失败的测试，再写最小实现使其通过
- 静态资源替换、纯文案、样式微调、无逻辑组件组合和配置接线优先使用构建、类型检查或
  其他可观察命令验证，不为每个需求机械新增测试
- 除非文件路径、DOM 数量、CSS 类名、源码文本或内部目录本身是明确的稳定契约或高风险
  回归点，否则不以这些实现细节作为永久测试的唯一断言
- 同一共享事实只在真实所有者领域验证一次，不在 Client、Admin 等消费端重复断言；两个应用
  各自独立的运行行为仍分别测试
- 修改已有能力时按真实契约更新所属测试，不遵循“一项需求对应一条测试”；发现只保护实现
  细节、与其他用例重复且没有独立回归价值的测试时应删除
- 测试按被测领域归入 `tests/<domain>/`，不按 unit/integration 平铺分类；`tests/` 根目录不得直接放 `*.test.ts` 或 `*.test.tsx`
- 领域测试不得相对导入其他领域测试；跨领域夹具放 `tests/support/`，只服务单一领域的 helper 留在当前领域
- 单个 `*.test.ts` 或 `*.test.tsx` 不得超过 2000 行；超出时按行为与生命周期边界拆分并提取领域 support
- 拆分测试禁止使用 `part-1`、`part-2` 等无语义文件名，也不能复制整套 fixture 只为降低单文件行数
- 测试需要隔离用户或工作区路径时显式注入临时根目录，不修改 `HOME`；其他环境变量改动保存原值并在 `finally` 中恢复
- 根 `pnpm test` 运行全部 Vitest；单一领域修改优先执行 `pnpm exec vitest run tests/<domain>`，再按影响范围决定完整验证
- `pnpm test:linter` 验证显式出口、接口注释、模块目录、私有成员、React 组件、Hook 顺序、className 和测试目录守卫
- 修改自定义 linter 时先运行对应测试文件，完成后再执行 `pnpm test:linter`；普通功能改动和创建 commit
  不额外执行 `pnpm test:linter`，除非用户明确要求
- 生产源码不通过 default export、星号导出、深层路径或缺少命名出口绕过质量守卫；接口及其成员职责注释必须完整
- 生成的 `projects/client/src/routeTree.gen.ts` 与 `projects/admin/src/routeTree.gen.ts` 不手工编辑，并继续排除于格式化、拼写检查、ESLint 和自定义质量守卫
- 新增或调整文件路由只修改 `src/routes` 下的源文件，通过开发或构建命令重新生成路由树，再核对生成 diff
- 日常开发只对本轮修改文件执行不带 `--fix` 的 ESLint 和相关测试；准备提交前按公共类型、包出口和调用链影响决定是否运行 `pnpm typecheck`
- 创建普通 commit 前不额外重复执行 ESLint 或全仓检查；提交阶段由 `.husky/pre-commit` 对已暂存的
  JavaScript / TypeScript 文件执行 `eslint --cache --fix`
- 只有准备推送、合并、创建 PR 或用户明确要求时才额外执行全仓 `pnpm lint`、`pnpm typecheck`、`pnpm test`
- 文档-only 修改不需要运行 `pnpm lint` 或 `pnpm typecheck`，但必须执行 `pnpm spellcheck` 和 `git diff --check`
- 修改源码后检查本次变更文件的行长：超过 90 字符就考虑换行，超过 120 字符除非拆分明显降低可读性，否则必须调整；import 行不在此限制内
- 换行不能以新增无业务语义的变量、常量、helper 或包装函数为代价，单行表达更清楚时保留单行
- 只检查本次修改的 `.ts`、`.tsx`、`.js` 源码文件，不做全量行长扫描；使用 Node 统计字符，避免中文在 `awk` 中按字节计数

```bash
node - <本次修改文件...> <<'NODE'
const fs = require('node:fs')
process.argv.slice(2).forEach((file) => {
  fs.readFileSync(file, 'utf8').split(/\r?\n/).forEach((line, index) => {
    if (/^\s*import\s/.test(line))
      return
    if (line.length > 90)
      console.log(`${file}:${index + 1}${line.length > 120 ? ' !' : ''}`)
  })
})
NODE
```

- 验证失败不能用兜底实现、跳过测试或白名单掩盖；记录失败命令、原因和已通过的检查

<!-- CODEGRAPH_START -->
## CodeGraph

In repositories indexed by CodeGraph (a `.codegraph/` directory exists at the repo root), reach for it BEFORE grep/find or reading files when you need to understand or locate code:

- **MCP tool** (when available): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current line-numbered source. If it's listed but deferred, load it by name via tool search.
- **Shell** (always works): `codegraph explore "<symbol names or question>"` prints the same output.

If there is no `.codegraph/` directory, skip CodeGraph entirely — indexing is the user's decision.
<!-- CODEGRAPH_END -->
