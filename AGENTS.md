<!-- cspell:words CodeGraph -->
# 工程规范

## 回复语言

- 默认使用中文回复

## 界面文案

- 用户界面的标题、按钮、说明、提示等文案结尾不写标点符号
- 面向用户的标题、说明、状态、错误和操作文案优先描述用户目标、可获得的能力、实际影响和下一步操作，尽量不暴露内部实现细节
- 除非诊断问题、安全确认或高级配置确有必要，不在用户文案中直接使用进程、PATH、RPC、轮询、协议或内部目录结构等实现概念
- 必须说明技术边界时，将实现约束转换成用户可理解的结果，例如使用“重新打开终端后生效”，不使用“进程环境变量已更新”
- 面向用户的错误和可恢复异常统一按“原因 → 影响 → 操作”组织，不暴露内部堆栈、配置、错误码或敏感输入
- 同一异常在页面、弹窗和提示中的核心表述保持一致，只能按容器信息密度裁剪，不能改变原因、影响或操作语义

## Git 提交与工作区保护

- Git 提交信息默认使用中文，避免使用英文描述
- 提交格式遵守 conventional commits，例如 `feat: 中文描述`、`fix: 中文描述`；type/scope 使用英文约定，冒号后的主题默认中文
- 提交前不手工执行 `eslint --fix`、`pnpm lint:fix` 或同义自动修复命令；`.husky/pre-commit` 由 `lint-staged` 对已暂存的 JavaScript / TypeScript 文件执行 `eslint --cache --fix`
- 同一文件同时存在已暂存与未暂存改动时，提交前分别核对 index 和工作区；`lint-staged` 会临时隐藏未暂存内容，提交后必须确认这些内容已恢复
- 开始修改前先检查工作区，现有修改和未跟踪文件默认属于用户；不覆盖、不清理、不暂存与当前任务无关的内容
- 提交前分别核对 `git diff`、`git diff --cached` 和 `git status --short`，只提交当前任务明确涉及的最小文件集合
- 除非用户明确要求，不执行 `git push`；禁止强制推送、重写用户提交或擅自修改分支上游
- 不提交构建产物、缓存、本地环境文件、密钥、访问令牌、真实个人信息或未跟踪的 `.codegraph/`

## 项目环境与命令

- 当前仓库使用 Node.js `24.18.1`、pnpm `11.18.0` workspace 与 Turborepo，依赖和根脚本统一从根目录 `package.json` 维护
- 不新增 `npm`、`yarn` 相关锁文件或脚本说明，跨 workspace 命令优先通过根脚本或 `pnpm --filter` 执行
- 根工作区只包含 `packages/*` 和 `projects/*`；`packages` 承载可复用框架能力，`projects` 承载可运行应用
- 服务端从根目录执行 `pnpm dev:server` 或在完成构建后执行 `pnpm start`，读取 `projects/server/.env` 作为运行配置，默认监听 `127.0.0.1:4317`
- 客户端从根目录执行 `pnpm dev:client`，默认监听 `4318`；管理端执行 `pnpm dev:admin`，默认监听 `4319`
- 客户端和管理端开发服务器把 `/api` 代理到服务端；生产环境默认使用同源 API，只通过非空 `VITE_API_BASE_URL` 显式覆盖
- 常规验证优先使用根目录脚本：`pnpm test`、`pnpm test:linter`、`pnpm typecheck`、`pnpm lint`、`pnpm spellcheck`
- 只改前端界面且确需人工确认交互时，可以同时启动对应前端和服务端；如果已有服务占用端口，先确认进程用途，再选择复用或调整端口
- `.env`、`.env.local` 等本地环境文件由 `.gitignore` 忽略，说明配置时只引用 `.env.example`，不把真实配置写进文档、提交信息或回复

## 项目边界与架构演进

- 当前阶段只维护三端基础框架和唯一的 `meta.info` 技术契约，不实现登录、课程、用户、权限、作业或其他业务页面和逻辑
- 不引入与当前三端技术骨架无关的运行形态、领域框架、持久化设施或其他项目品牌残留
- `packages/api` 负责共享契约与 Schema，`packages/request` 负责统一请求和错误边界，`packages/sdk` 负责类型化客户端，`packages/shared` 负责环境无关工具，`packages/shared-ui` 负责共享主题、样式和 UI 能力
- `projects/server` 只承载服务端应用，`projects/client` 只承载客户端应用，`projects/admin` 只承载管理端应用；可复用能力不得长期沉积在应用目录
- 新功能、问题诊断和重构先根据当前源码、配置和测试确认真实边界，不根据记忆猜测接口、状态或生命周期
- 发现现有边界无法支撑目标时优先修正职责和数据所有权，删除重复实现、无效封装和残留分支，不通过新增兜底状态、旁路结构或一次性 helper 叠加补丁
- 当前项目处于未发布开发阶段，内部架构变更默认不保留兼容；旧出口、旧类型和旧入口直接删除并同步全部调用方与测试，除非用户明确要求兼容
- 当前阶段以职责单一、依赖清晰和总代码量可控为优先级，不为未来可能需要的能力预建兼容层、空抽象、占位实现或空目录
- 同一能力只保留一套清晰 API，不为了兼容或显得完整并行暴露重复的注册、触发或访问入口
- 每轮功能完成后检查重复实现、无效封装、薄转发层、空源码目录和可删除代码，发现后在当前范围内直接清理
- 能从源码、配置和测试确认的事实先主动探索；涉及产品意图、架构取舍或高影响决策且无法从仓库确定时，及时询问用户，不静默猜测或切换到实质不同的实现
- 问题诊断、新需求方案、架构、重构和全仓治理计划默认按可独立验证和提交的批次组织，不把批次编号当作问题严重度或文件大小等级

## 安全边界

- 清理缓存、临时目录或生成物前必须先确认 `pwd`、目标绝对路径和目录内容，不能凭变量名或记忆直接删除
- 不使用宽泛的 `rm -rf` 清理数据；必须删除时只删除明确的叶子目录或文件，并优先先展示目标
- 删除用户目录、仓库根目录、`projects`、`packages`、`docs` 等高风险路径前必须向用户确认，不能把局部清理扩大为删除源码或工作区
- 涉及删除、迁移、重命名目录时，先说明目标路径和影响范围，再执行最小范围操作
- 删除或覆盖文件前核对绝对路径和影响范围，只操作已确认的目标；优先使用可恢复操作
- 不提交密钥、口令、访问令牌、真实个人信息或包含这些内容的日志；环境变量样例只保留字段名和安全占位值
- 外部输入在 HTTP、配置和文件等边界先收窄或校验，错误响应不得暴露内部堆栈、配置内容或敏感输入
- 检查因缺少外部服务、密钥或端口占用失败时，不用兜底代码掩盖，记录失败命令、原因和已经通过的边界

## 注释风格

- 函数声明、class 方法和有实现体的箭头函数上方的块注释只写职责，通常只写一行，不在职责注释里展开流程细节
- 函数声明、class 方法和文件级箭头函数使用多行块注释，即使只有一行说明也写成三行

```typescript
/**
 * 启动服务并返回关闭句柄
 */
export function startServer() {}
```

- 函数内部局部箭头函数的职责注释统一使用单行块注释 `/** ... */`，不按复杂度扩成三行
- 类型、接口、常量、普通字段、接口成员和对象类型成员需要写职责注释，并使用单行块注释 `/** ... */`
- 公共类型、公共接口、导出函数与复杂状态必须写清职责和边界，不重复解释代码字面含义
- `useState`、`useRef`、`useMemo` 等声明状态、引用或派生值时，语义不明显就用单行块注释说明
- Store 或 Hook 解构、网络请求结果和重要派生值如果牵涉共享状态、SDK 投影或本地 UI 状态，必须说明数据语义和权威来源
- 函数内部需要补充说明时使用 `//` 行注释，解释实现意图、边界条件、状态变化或失败原因，不把行注释当函数标题
- 复杂函数的执行流程、数据来源、合并顺序、并发边界和失败边界放在函数体最上方或关键分支前，不堆在函数职责注释里
- 状态同步、缓存合并、服务端快照校准、异步竞态和协议转换等容易误读的逻辑，要说明权威来源、覆盖顺序和失败时保留的状态
- 不使用带序号的步骤注释，避免代码调整后序号漂移
- 有实际实现或类型聚合的源码文件按需保留结构分区标题，例如 `/** -------------------- 类型 -------------------- */`、`/** -------------------- 常量 -------------------- */`、`/** -------------------- 核心函数 -------------------- */`、`/** -------------------- 内部函数 -------------------- */`
- class 内方法较多时使用 `/** -------------------- 核心方法 -------------------- */` 和 `/** -------------------- 辅助方法 -------------------- */` 区分对外主行为和内部逻辑
- 分区标题只用于文件结构，不用于函数内部步骤；标题与下一条注释之间不留空行
- 文件内容通常按类型、常量、核心函数、内部函数排列
- 中文注释末尾不写句号；注释要适量，不写“设置 name”“调用 save”这类只重复代码字面含义的内容

## TypeScript 与代码风格

- 使用严格 TypeScript，优先沿用现有结构和命名，小范围修改不顺手重构无关代码
- 优先让 TypeScript 推断返回值和局部变量类型，公共 API、`any` 风险或复杂返回边界再显式标注
- 不使用 `any` 绕过类型检查；不确定的外部输入先作为 `unknown` 收窄或通过 Zod 校验
- 对象结构优先使用 `interface`，联合类型、工具类型组合和非对象结构再使用 `type`
- 不写只包一层已有类型的空接口或别名；类型能安全内联就内联，跨多处复用或属于公共契约时再提取
- 入口校验优先放在服务端 Schema、Zod 默认值或模块公共入口，内部调用链不重复抛出相同的缺参错误
- 字段语义未变化时在 selector、Hook、局部派生、对象装配和返回值中保留原名，不添加重复当前上下文的前缀
- 同一作用域既保留原始值又生成同名权威值时，前序临时值使用 `_value` 这类短名称，最终值保留自然名称并使用对象属性简写
- 从对象读取多个字段用于同名局部变量或同名对象字段时优先解构；字段需要大量重命名、默认值或生命周期判断时直接读取源对象更清楚
- 函数体顶层读取参数属性时形成连续声明区，位于 Hook 和其他运行时语句之前；只服务条件分支的读取留在窄作用域
- 解构包含少量短字段时保持单行；字段多、别名长、默认值复杂或一行拥挤时再按字段换行
- 字段进入下游后语义变化时在解构处直接重命名并继续使用属性简写，不把重命名拖到最终对象投影
- 只有原始值和转换值需要同时存在时才使用 `inputXxx`、`rawXxx` 等来源名称，不为单纯转发创建中间别名
- 类同时提供静态 `create()` 和 `withXxx()` 时，静态入口负责规范化输入并创建共享依赖，私有构造函数只接收完整上下文，不判断当前是首次还是派生创建
- 创建完整上下文时，先在连续声明区准备入口字段、派生值、集合和依赖，最后用纯字段投影创建实例，不在装配对象里内联复杂转换
- `withXxx()` 只重算真正变化的字段，共享生命周期资源使用同名局部变量，最终以纯字段投影创建新实例
- 私有构造函数先解构完整上下文，需要规范化的输入在解构处命名为 `inputXxx`，再计算最终同名变量并连续写入实例字段
- 上述装配规则不适用于单次属性读取或直接委托；直接表达更清楚时不要机械增加只用一次的局部变量
- 数组解构一行放不下时，每个元素独占一行，收口写成 `] = 调用(`；不要把 `=` 单独放到下一行
- 对 store、ref、缓存对象等运行中会变化的对象，不在外层提前解构易变字段；需要当前快照时在使用点窄作用域读取
- 类型断言使调用参数变长或需要多段断言时，先声明有语义的局部变量再传入
- 可选布尔参数默认是 `false` 时不额外写 `= false`
- Promise 必须通过 `await`、`return` 或显式拒绝处理完成归属；不使用 `void` 忽略 Promise，确需后台执行时在调用点补 `.catch()` 并处理失败
- 第三方库缺类型时优先使用官方类型或 `@types/*`，确实没有可用类型时才在当前 workspace 内补最小 `.d.ts`
- 不为一两行简单逻辑提取 helper，尤其不创建只包一层字符串或类型判断的函数
- 文件级 helper 只用于跨多处复用、隐藏稳定边界、隔离协议转换或明显改善模块结构；只被一个函数使用的局部步骤优先收进调用方
- 普通函数内部的局部步骤使用箭头函数，React 组件事件函数使用箭头函数，与组件状态无关的转换使用函数声明并放在最近的 `utils.ts` 或实现模块
- 一个主函数包含多段可独立命名的局部逻辑时，用内部箭头函数组织，让文件级结构只呈现稳定主边界
- 顶层函数只保留对外 API、跨多处复用的能力或稳定主流程；不因“只调用一次”机械嵌套整条复杂调用链
- 一次输入到输出的同步转换即使有临时可变状态也优先使用函数；只有对象需要身份或状态跨多个操作和生命周期持续存在时才使用 class
- 相邻局部函数之间保留空行，职责注释紧贴对应函数
- 函数和方法参数短且清楚时保持单行；参数多、嵌套深或信息密度高时按参数逐行展开
- 对象解构参数优先在括号内展开字段，类型标注跟在收口花括号后
- 函数调用参数较多或较长时逐个换行；短调用、短条件和短链式表达式单行更清楚时保持单行
- JSX 开标签只有少量短 prop 且一行可读时保持单行；复杂 prop 先在 `return` 前声明语义变量
- 简单判断可单行表达，复杂判断先声明布尔语义变量；`for...of` 用于副作用、异步或多步骤控制流，高阶函数用于简单映射、过滤和查找
- 空行只分隔不同语义块，连续相关语句不为“透气”额外留空行
- class 对外方法显式写 `public`，私有方法和私有字段使用 `_` 前缀
- 除错误类型、JavaScript / 平台 / 第三方对象和框架要求直接构造的对象外，业务调用方不对项目自定义 class 使用 `new`；普通实例通过 `public static create()` 创建，构造函数保持 `private`
- class 构造函数只保存入参时优先使用构造函数参数属性；使用参数属性时不逐个写注释，在 constructor 上方说明职责

## 目录、模块与公开出口

- 源码模块目录除 `index.ts` 出口外，避免同时混放普通文件和子目录；同层实现能保持单文件时使用 `[name].ts` 平铺，不创建只有一个 `index.ts` 的机械叶子目录
- 同级存在真实多文件模块、资源目录或明确能力边界时使用 `[name]/index.ts`，当前目录只保留聚合出口
- 模块目录按真实职责创建 `types.ts`、`schema.ts`、`utils.ts`、`hooks.ts` 等文件，不为目录对称创建空文件或薄转发层
- `index.ts` 只承担稳定出口、薄门面或必要的主流程编排，不为了缩短入口拆出大量一次性 helper
- 生产源码禁止默认导出和任何星号导出，包括 `export * from`、`export type * from`、`export * as Xxx from`；框架要求默认导出的工程配置文件除外
- 公共出口使用 `export { ... }` 和 `export type { ... }` 明确列出契约，新增公开符号必须从对应 `src/index.ts` 显式导出
- 新增包或公开子路径时同步更新对应 `package.json#exports` 和根 `tsconfig.json#paths`；测试或应用有独立别名配置时一并更新
- workspace `tsconfig.json` 保持薄继承，只 include 当前 workspace 源码和必要配置；仅在真实依赖相邻包全局声明时扩展 include
- 只被单个实现使用的类型留在实现文件；同一领域多文件共享类型放在模块 `types.ts`，跨模块公共契约通过领域出口导出
- 不维护混合多个领域所有者的巨型 `types.ts`，也不为单个接口创建 `Interface.ts`
- 跨 workspace 必须从 `@socilab/api`、`@socilab/request`、`@socilab/sdk`、`@socilab/shared`、`@socilab/shared-ui` 等显式公开出口导入
- workspace 之间禁止使用 `../../` 穿透源码，也禁止通过 `@socilab/<package>/src/...` 绕过公共契约
- 同一 workspace 内相邻实现可使用相对导入；包出口聚合和明确内部单向依赖不通过自包 barrel 绕行，避免循环依赖
- 通用能力优先放在职责匹配的共享包内实现和复用，不为一两个函数引入大型运行时依赖
- 只有一层实现且没有真实替换点时，不拆 adapter、factory 或 define 透传层，直接暴露可用边界

## React 与 TSX

- 前端沿用 React 19、TanStack Query、TanStack Router、React Compiler、Tailwind 与 `@socilab/shared-ui`，不为局部需求引入第二套 UI、路由、请求或状态方案
- React Hooks、`Fragment`、`ReactNode` 等从 `react` 具名导入，避免在 JSX 和组件逻辑里写 `React.xxx`
- React 组件使用 `function Xxx(props: XxxProps) {}` 声明，不使用 `FC`、箭头函数或函数表达式；事件和 JSX 回调继续使用箭头函数
- 泛型函数组件使用 `function Component<T>()`，不强套 `FC`，避免削弱泛型推断
- TSX 文件只承载界面和紧邻交互；纯数据转换、解析和格式化逻辑移到相邻 `utils.ts` 或所属框架包
- 一个 `*.tsx` 文件默认只放一个主组件；同一抽象层的小组件最多两个，继续增长时按真实组件边界拆分，不预建空目录
- 组件 Props 接口使用 `[组件名称]Props`，不使用裸 `Props`；组件复杂后再按实际内容拆出 `props.ts`、`types.ts`、`constants.ts`、`utils.ts`、`hooks.ts`
- 点击、键盘、拖放和提交等依赖组件闭包的事件函数保留在组件中，不为了拆文件包装成事件 helper
- 简单 `useState`、`useRef` 和只服务一处 JSX 的 Hook 留在组件；具有完整订阅、观察和清理生命周期且可独立命名的逻辑再提取自定义 Hook
- 浏览器 Object URL、Observer 和订阅句柄由创建它们的状态所有者管理，在删除、切换、成功提交和卸载时释放，不进入协议或持久化状态
- Hook 只在组件或自定义 Hook 顶层调用，调用顺序保持为：通用 Hook → state/ref Hook → memo Hook → 普通派生值与事件函数 → `useEffectEvent` → Effect 类 Hook → `return`
- `useEffectEvent` 与 Effect 分别集中排列，不交错声明；它只供 Effect 调用，不代替点击、提交等普通事件
- Effect 中需要读取最新状态但不应触发重新订阅时先声明 Effect Event；不能用 Effect Event 隐藏真实响应式依赖
- 简单 Effect 保持单行；多步副作用或异步流程使用 Effect 内的命名局部函数，并明确清理和过期结果边界
- Hook 依赖数组只有一个依赖时保持单行，依赖多或名称长时再逐项换行
- 用户触发的异步操作和按钮 loading 优先使用 TanStack Query `useMutation`；校验留在提交边界，请求成功后再关闭交互容器
- 不因为引用稳定性机械添加 `useMemo`、`useCallback` 或 `memo`；只在第三方依赖敏感、存在明确性能问题或计算成本较高时使用
- 客户端和管理端始终启用 React Compiler，不用永久记忆化或 ESLint 白名单掩盖可修复的编译问题
- 事件处理直接调用函数，不写 `void someHandler()` 或 `void Promise`；参数能推断时不手写事件类型
- JSX 中单纯的条件展示使用 `condition && <Component />`；非布尔值先显式布尔化，互斥分支继续使用三元表达式
- JSX 属性或 children 中的复杂表达式需要多行时，大括号独立换行承载表达式，不写半行后再换行
- JSX 不内联业务计算、复杂事件函数或复杂对象；按“前置守卫 → 派生值 → 事件函数 → ReactNode / Props 对象 → `return`”组织
- Effect 专属流程放回对应 Effect，复杂渲染分支优先拆组件，不抽局部 `renderXxx`
- 默认不启动浏览器工具做页面验证；只有用户在当前任务中明确要求时才执行

## Tailwind 与 className

- 业务 UI 优先使用 Tailwind utility class；只有主题入口、`@theme` / `@utility`、第三方全局覆盖或确属全局样式入口时才维护 CSS
- 共享样式和主题变量放入 `@socilab/shared-ui`，客户端和管理端不复制第二套主题；当前只维护浅色主题基线
- `styles` 等非组件资源目录按职责使用平铺文件，例如 `theme.css`、`base.css`，不为单个 `index.css` 创建机械目录
- 前端布局、间距、字号、圆角和控件尺寸优先使用 Tailwind scale 或稳定主题 token，不在 JSX、`className` 和业务 CSS 中散落固定像素或一次性 `rem`
- 发丝线、动态 DOM 测量、CSS 函数和第三方组件明确比例等真实例外可以使用精确值，不机械替换
- 组件样式优先写入 `className`；只有动态数值、第三方组件只暴露行内入口或运行时计算更清楚时才使用 `style`
- `className` 足够短时保持单行；utility 较多时使用 `@socilab/shared-ui` 的 `cn(...)` 按布局、尺寸、边框、字体、状态和外部覆盖分组
- 一个静态类名字符串不使用 `cn(...)` 包装；只服务单个 JSX 片段的一次性 className 不提取成常量
- 合并多段静态或条件样式统一使用 `cn`，不使用数组 `join(' ')`、`filter(Boolean)`、字符串拼接或条件模板字符串
- 条件类名优先使用 `condition && '...'` 或条件对象，整段 class 的简单三元选择可以保留
- 组件透传的 `className` 放在 `cn(...)` 最后，允许调用方覆盖前面的 Tailwind 冲突类
- Tailwind 尺寸、间距、圆角、字号和断点优先使用 scale utility，不使用任意像素断点；细间距使用数字 scale
- 只有真实依赖外部协议、第三方内部选择器或 CSS 函数无法用 scale 表达时才使用 arbitrary class
- Tailwind 4 类名保持 canonical 写法；修改类名后处理无效 utility、冲突和可替换的旧写法
- `z-index` 按语义分层：组件内部使用低层级，页面覆盖层、模态遮罩和 Portal 内容逐级提高；遮罩与弹层内容不得依赖同层 DOM 顺序决定点击归属
- `min-h-0` 只用于 flex/grid 子项确需突破默认最小高度承载滚动或收缩的场景，不作为普通容器默认类
- `cn(...)` 多行分组先保证语义一致，再让每行长度和信息密度尽量接近，不为均分字符打散复杂选择器

## 测试与质量门禁

- 实现行为变更先写能失败的测试，再写最小实现使其通过；配置和纯文档修改使用可观察命令验证，不制造无意义的运行时 RED
- 测试按被测领域归入 `tests/<domain>/`，不按 unit/integration 平铺分类；`tests/` 根目录不得直接放 `*.test.ts` 或 `*.test.tsx`
- 领域测试不得相对导入其他领域测试；跨领域夹具放 `tests/support/`，只服务单一领域的 helper 留在当前领域
- 测试需要隔离用户或工作区路径时显式注入临时根目录，不修改 `HOME`；其他环境变量改动保存原值并在 `finally` 中恢复
- 根 `pnpm test` 运行全部 Vitest；单一领域修改优先执行 `pnpm exec vitest run tests/<domain>`，再按影响范围决定完整验证
- `pnpm test:linter` 验证显式出口、接口注释、模块目录、私有成员、React 组件、Hook 顺序、className 和测试目录守卫
- 生产源码不通过 default export、星号导出、深层路径或缺少命名出口绕过质量守卫；接口及其成员职责注释必须完整
- 生成的 `projects/client/src/routeTree.gen.ts` 与 `projects/admin/src/routeTree.gen.ts` 不手工编辑，并继续排除于格式化、拼写检查、ESLint 和自定义质量守卫
- 新增或调整文件路由只修改 `src/routes` 下的源文件，通过开发或构建命令重新生成路由树，再核对生成 diff
- 日常开发只对本轮修改文件执行不带 `--fix` 的 ESLint 和相关测试；准备提交前按公共类型、包出口和调用链影响决定是否运行 `pnpm typecheck`
- 只有准备推送、合并、创建 PR 或用户明确要求时才额外执行全仓 `pnpm lint`、`pnpm typecheck`、`pnpm test`
- 文档-only 修改不需要运行 `pnpm lint` 或 `pnpm typecheck`，但必须执行 `pnpm spellcheck` 和 `git diff --check`
- 修改源码后检查本次变更文件的行长：超过 90 字符就考虑换行，超过 120 字符除非拆分明显降低可读性，否则必须调整；import 行不在此限制内
- 换行不能以新增无业务语义的变量、常量、helper 或包装函数为代价，单行表达更清楚时保留单行
- 验证失败不能用兜底实现、跳过测试或白名单掩盖；记录失败命令、原因和已通过的检查

<!-- CODEGRAPH_START -->
## CodeGraph

In repositories indexed by CodeGraph (a `.codegraph/` directory exists at the repo root), reach for it BEFORE grep/find or reading files when you need to understand or locate code:

- **MCP tool** (when available): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current line-numbered source. If it's listed but deferred, load it by name via tool search.
- **Shell** (always works): `codegraph explore "<symbol names or question>"` prints the same output.

If there is no `.codegraph/` directory, skip CodeGraph entirely — indexing is the user's decision.
<!-- CODEGRAPH_END -->
