const REUSED_CLASS_NAME = 'flex gap-sm'
const REUSED_CLASS_NAMES = { item: 'px-sm' }

export const view = (
  <>
    <div className={REUSED_CLASS_NAME} />
    <span className={REUSED_CLASS_NAME} />
    <Menu classNames={REUSED_CLASS_NAMES} />
    <Menu classNames={REUSED_CLASS_NAMES} />
    <Button classNames={{ icon: 'size-4', root: 'w-full' }} />
    <Tooltip rootClassName="pointer-events-none" />
    <div
      className={cn(
        'flex min-h-0 gap-sm overflow-auto',
        active && 'bg-muted',
      )}
    />
    <div className="[&_.library-very-long-semantic-selector_with_nested_state]:overflow-hidden" />
  </>
)
