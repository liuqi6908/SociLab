const classes = 'bg-' + globalThis.name

export function FirstComponent() {
  const classes = 'flex'

  return <div className={classes} />
}

export function SecondComponent({ color }: { color: string }) {
  const classes = 'bg-' + color

  return <span data-class-preview={classes} />
}

export function NestedScope({ color }: { color: string }) {
  const classes = 'grid'
  const readPreview = () => {
    const classes = `text-${color}`

    return classes
  }

  return <div className={classes} data-preview={readPreview()} />
}

export function ComponentParameter({ classes }: { classes: string }) {
  return <div className={classes} />
}

export function CallbackParameter() {
  const render = (classes: string) => <span className={classes} />

  return render('grid')
}
