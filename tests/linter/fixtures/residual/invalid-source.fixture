import { createAgent } from '@qygent/agent'
import { startRuntime } from '@socilab/runtime'
import { createThread } from '../../thread/index'
import electronBuilder from 'electron-builder'
import electronPackager from '@electron/packager'

import LegacyPluginModule = require('@socilab/plugin')

export type LegacyThreadType = import('@socilab/thread').LegacyType

export const legacyAgentModule = require('@socilab/agent')
export const lazyElectronModule = import('@socilab/electron')
export const product = 'QiyanAgent'
export const formerCompany = 'QiyanSoft'
export interface Agent {}
export const Electron = 'legacy-shell'
export const values = [
  createAgent,
  electronBuilder,
  electronPackager,
  LegacyPluginModule,
  createThread,
  startRuntime,
]
