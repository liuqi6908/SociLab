const protocolModule = import(`../protocol/template`)
const sharedModule = require(`../shared/template`)
const domain = 'server'
const unknownModule = import(`../${domain}/template`)
const unknownCommonJsModule = require(`../${domain}/template`)

export {
  protocolModule,
  sharedModule,
  unknownCommonJsModule,
  unknownModule,
}
